

class Authentication:
    def __init__(self):
        # self.users =  db_reader()
        pass

    def authenticate(self, username, password):
        # if username in self.users and self.users[username]["password"] == password:
        if True:
            return True
        return False